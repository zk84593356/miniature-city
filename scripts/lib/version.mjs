/**
 * lib/version.mjs — the skill version these scripts were copied from.
 *
 * A project's scripts/ and tools/ are COPIES of the skill's; nothing syncs them.
 * Every script prints this under --help / --version so a copy that fell behind
 * can be told apart from the skill (selftest pins it to SKILL.md's frontmatter).
 */
export const SKILL_VERSION = "0.3.23";
