# Step 0 范围判定

- 目标：`https://cityinminiatures.top/`
- 判级：**A（完全适用）**
- 一句话断点：无断点；目标是确定性的静态 Vite 单页，签名行为和数据加载逻辑都在客户端主包中，可走“镜像 → 行号逆向 → 严格移植 → 定量验收”主线。

## 关键证据

1. 路径级 GET 返回 200，最终 URL 仍为 `https://cityinminiatures.top/`，无重定向。
2. 两次成功取得的 HTML 字节完全一致，均为 3100 字节；SHA-256 为 `0aa68ad1e3c46214689e805f8efc78e11e8ad431c9142e34c7d801e66fc03acd`。
3. HTML 只引用一个主模块 `/assets/index-zfVzkv9E.js` 和一个样式表 `/assets/index-CJdN52Ic.css`，未命中 Next RSC、React Router framework、Nuxt、Vue scoped、R3F 或 Theatre 的框架标记。
4. 主包已成功取得，大小 1,020,907 字节，SHA-256 为 `bd2411b90aff0e56ec59467180f71fd8d9e0cc7f660666a8db2668a393072f5e`；无 sourcemap 注释。
5. 主包包含 three.js `WebGLRenderer`、OrbitControls、ShaderMaterial 等强签名，并有应用层命令式类负责相机飞行、碰撞高度、光照、昼夜、地点导航和声音；不是仅含序列化结果或声明式空壳。
6. 真实浏览器基线成功渲染三维深圳全域，包含 5 个区域入口、19 个地点、昼景/日落/夜景、地图缩放、相机复位、沉浸模式、截图、显示设置、小地图、中英文和背景声音。
7. bundle 字面量显示运行时数据资产包括地形、建筑、路面、夜景、交通与实例化城市二进制，以及背景音乐；未发现 `/api/` 字面量。此项只作 Step 0 假设，是否存在运行时 API 仍须由 M0 浏览器补录确认。

## 三判据

- 内容可镜像性：是。成功样本字节一致，静态 HTML/JS/CSS 外壳明确。
- 签名交互承载层：是。three.js/WebGL 与命令式相机、光照、导航逻辑都在可下载主包中。
- 客户端持有行为源：是。未发现 RSC flight 或仅服务端序列化输出的结构。

## 风险与待办

- 目标站网络响应慢且偶发连接超时；这属于采集环境噪声，M0 必须保留重试与逐文件哈希账本。
- `robots.txt` 本次读取超时，未得到规则文本。进入 M0 前必须再次读取并按路径解释；“超时”不等于允许或禁止。
- Step 0 的 token 计数尚未剔除 vendor，不能用于最终技术栈版本和难度结论；这些结论要在 M1 的 pretty 行号坐标系中复核。
- 交付默认私有、`noindex`、不部署；公开使用的版权与归属决定须在收口阶段交回用户。
