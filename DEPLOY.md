# 发布与权利事实表

> 本文件只记录可验证事实和风险选择，不构成法律意见。项目所有者已于 2026-09-17 选择 **B：制作可公开衍生版**，并确认拥有应用代码、三维资产和数据文件的公开使用授权。当前产物仍是**本地预览、`noindex`、未公开部署**；文案与声景的授权凭证仍应在正式上线前归档，或以自有素材替换。

## 1. 当前分发事实

- 产物目录：`site/`，共 20 个文件、28,716,846 字节。
- 运行方式：本地静态服务；不需要安装 npm 运行依赖。
- 公开状态：未部署到公网；当前目录也不是 Git 仓库，没有可见的 push/发布记录。
- 搜索索引：入口页已注入 `noindex, nofollow, noarchive`。
- 网络依赖：站点运行资产均在本地；原站抓包中的 Cloudflare analytics 被接受为非功能性降级，并由本地服务 stub。
- 运行代码：前端 hash chunk 与源站镜像逐字一致；`src/readable/` 只是可重组的研究视图，不改变浏览器执行代码。
- 公开衍生版：`public-site/`；完整保留深圳 3D 运行资产，只对两份文字文件应用登记变换，并叠加署名、隐私、404、响应头与城市清单。
- 发布边界：只发布 `public-site/`；不得把 `mirror/`、`site/`、`src/readable/` 或逆向笔记作为网站根目录公开。

## 2. 素材与来源清单

| 类别 | 文件数 / 字节 | 用途 | 站内可见证据 | 权利或许可状态 | 公开前建议 |
|---|---:|---|---|---|---|
| HTML / CSS / JavaScript | 5 / 1,068,496 | 页面、界面、Three.js 场景和错误页 | bundle 中出现 `three.js`；无 source map | 应用代码公开使用授权已由项目所有者确认；Three.js MIT 署名需保留 | 归档授权凭证与 Three.js 许可文本 |
| 城市 JSON / 二进制数据 | 13 / 20,270,352 | 地形、建筑、活动、灯光、交通与实例数据 | 页面资料链接包含政府、百科、建筑资料站等 | 数据文件公开使用授权已由项目所有者确认；OSM/Mapzen 等第三方义务继续适用 | 归档授权凭证并保留逐项来源、许可与署名 |
| 音频 | 1 / 7,377,792 | 环境声景 `bay-breeze.mp3` | 文件内/页面未发现授权说明 | **[未确认]** | 取得授权或替换为自有/明确许可音频 |
| 地图署名与外链 | 页面内链接 | 来源说明 | 可见 `© OpenStreetMap contributors` 与 `Mapzen Terrain` 链接 | 署名存在；具体数据使用和再分发义务仍 **[未确认]** | 按实际数据链核对 ODbL/Mapzen 归属要求并保留署名 |
| 品牌、名称、文案与视觉表达 | 页面整体 | 深圳交互展示 | 平台品牌已替换为“微缩城市图志”；深圳及地名作为真实城市事实保留 | 平台品牌已分离；其余说明文案的授权凭证仍应归档或逐步重写 | 上线前完成文案清单并保存证据 |

> 字节统计包含：入口页 3,403；404 模板 79；前端 assets 1,065,014；城市数据 20,270,352；音频 7,377,792。`build-report.json` 206 字节属于构建元数据，不是用户素材。

## 3. 第三方标识与隐私扫描

- 严格模式扫描未发现 Google Analytics / Tag Manager、PostHog、Sentry、Vercel、Rewardful 或类似项目标识。
- 未发现邮箱、电话或 `mailto:` / `tel:` 联系标识。
- bundle 可识别 `three.js`、OpenStreetMap、Mapzen；仅发现一般性的 `copyright` 字样，没有随包附带的完整第三方许可清单。
- 原站网络账本记录了 `static.cloudflareinsights.com`；本地成品不主动连接它。

扫描结论只说明“当前文件中未命中已检查模式”，不等于完成所有隐私、许可或安全审计。

## 4. 混淆风险

如果把当前版本直接公开，页面名称、文案、视觉布局、三维城市资产和交互行为都与原站高度一致，访问者可能把它误认为原站、官方镜像或获授权版本。`noindex` 只能降低搜索曝光，不能消除品牌、著作权、数据许可或来源混淆风险。

## 5. 可选发布路径

| 选择 | 做法 | 主要风险 | 建议 |
|---|---|---|---|
| A. 保持私有 | 继续本地使用，不公开托管，不分发镜像/数据/音频 | 最低，但仍应限制文件流转 | **当前推荐与默认** |
| B. 制作可公开衍生版 | 更名、重写文案与视觉；替换音频和未确认数据；补齐许可/署名；再做功能与视觉验收 | 需要较多重制工作，仍需逐项确认来源 | 如确需公开，推荐走此路径 |
| C. 原样公开 | 直接托管现有 `site/` | 权利链和来源混淆风险最高 | 不建议；须先取得明确授权并完成专业审查 |

## 6. 项目所有者决定

公开决定：**选择 B，制作授权的公开衍生版**。

当前已完成品牌替换、授权声明、数据署名、隐私说明、安全响应头和多城市清单；深圳 3D 效果与非文字资产保持不变。`noindex` 将继续保留，直到确定正式域名、托管方式并完成一次线上预览验收。上线前应把授权凭证保存在不公开的项目档案中。
